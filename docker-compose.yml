# docker-compose.yml
services:
  postgres:
    image: postgres:16
    container_name: scope-postgres
    environment:
      POSTGRES_DB: scope
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"